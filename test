<?php

require_once __DIR__.'/../vendor/autoload.php';

use Skeletal\Kernel;

Skeletal::getInstance()->handleRequest(function () {
    // This runs before the request gets processed
    // but we also have the request object available here
    debug($this->app->request->path());

    return Kernel::process();
});
